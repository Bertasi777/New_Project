from django.shortcuts import render

from item.models import Cateroy, Item

def index(request):




def index(request):
    return render(request, 'index.html')

    def contact(request):
        return render(request, 'core/contact.html')
